# RESUME — CRISPRme+ session state (do not commit; scratch resume doc)

_Last updated mid-session; context near limit. Everything below is on disk / pushed unless noted._

## TL;DR
- **STABLE `v2.2.0` SHIPPED (2026-08-18).** `main` @ `0b2a786`, GitHub release `v2.2.0` (latest), Docker multi-arch publishing (run 32092429524). = the DICT-based line, all bugs closed: pre-2.2.0 audit (6 finders + parity, 17 findings) → 1 blocker = phased multi-SNP haplotype under-report, FIXED (PR #41, deferred-peel) + real-data no-regression gate; indel #172 FIXED (alpha.30) + real-data e2e (99 genomic-indel off-targets flow through). 16 minor audit items + the AF-column limitation are documented/deferred to the 2.2.x/2.3.0 backlog. `crisprme.py` version=`2.2.0` (alpha only ever lived in tags). See [[version-line-policy-2-1-x-vs-2-2-x]], [[phased-multisnp-haplotype-underreport]], [[indel-offtargets-silently-dropped-grep-fakechr]].
- **`v2.3.0` (dictless lineage) SHIPPED (2026-08-18).** `main` FF'd to `eb9821a` (= 2.2.0 + dictless), GitHub release `v2.3.0` (latest), Docker multi-arch publishing (run 32150472498). Dict-less engine (Tier-0 registry + Tier-1 genotype store replacing 152 GB dicts) + combination-aware population-summary output + corrected AF. Merge `8138dbc` (277 tests) → download gaps 2/3 fixed `c91fd79` → version bump `27f50ab`. Genome-wide tiers emitted on ml007 (23/23). Published dict-less index `NRG_3_hg38-dictless+hg38_1000G_HGDP` on HF; **dict-based `NRG_3`/`NGG_3` remain the DEFAULT** (dict-less is opt-in until a later default-flip). Frictionless new-user e2e PASSED on 2.3.0 (zero manual steps: canonical install + auto samplesID + registry gate + CPS1 w/ 90+ carriers from the genotype tier + 23 population_summary companions). See [[crisprme-plus-dictless-e2e-and-gaps]].
- **Open 2.3.x follow-ups:** Gap-2b (task #111 — `download --what index` should fetch its per-db samplesIDs; documented order for now); the DEFAULT-FLIP to dict-less (separate rollout decision); #99 Tier-0 compaction; #97; the 16 minor audit items.
- _(historical)_ Prior shipped pre-release was `v2.2.0-alpha.29/.30`; batteries variant search proven end-to-end (CPS1), tokenless, Docker + read-only apptainer, both NRG (default) + NGG indexes on HF with gzipped dicts.
- **Active dev = the DICTLESS redesign** on branch **`feat/tier0-registry`** (repo `/Users/lp698/Projects/crisprme-plus`), **UNMERGED**, `main`/alpha.29 untouched. HEAD commit `b3ac992` (C2a HF publish/download for tiers). 136 tests green. [chain: …3c `e26f8d2` → 3d build-emit `11f3a75`/`c2c766a` → #172 cherry-pick `9a37e73` → C2a publish/download `b3ac992`]
- **C2a DONE** (`b3ac992`): `crisprme_hf.py`/`crisprme.py` gained additive `--dictless` publish (main tarball = registry_+log_indels_, dicts excluded; separate `genotypes_<vcf>.tar.gz`) + `--no-genotypes` download; default path member-unchanged (proven); download places genotypes_ at the resolver path. Reviewed APPROVE, no issues.
- **Phase 3c DONE** (`e26f8d2`): population summary wired as an ADDITIVE companion `<output>.population_summary.tsv` (gated on registry, existing outputs byte-identical, reviewed APPROVE). Full population-summary feature (3a/3b/3c) complete.
- **Phase 3d DONE** (`11f3a75`+`c2c766a`): BUILD-side emission — `crisprme.py build-index-only` now emits the Tier-0 registry + Tier-1 genotype tier alongside the dicts (new `PostProcess/build_dictless_tiers.py` helper; new optional `--samplesID` arg → `_build_db_to_samplesid`). ADDITIVE (crisprme.py 0 deletions, dicts byte-unchanged), gated (usable --samplesID map + tier modules importable) + guarded (STDOUT warnings, never fatal), gz-aware. **Emitted paths byte-match the search resolvers** (`_resolve_registry_paths`/`_resolve_genotype_paths`) — verified both sides. Reviewed APPROVE.
- **Next action (ROLLOUT = OPTION 1, user-chosen 2026-08-17):** publish dictless under a NEW index name (e.g. `NRG_3_hg38+hg38_1000G_HGDP-dictless`) via `--dictless` (registry in main tarball + separate `genotypes_<vcf>.tar.gz`, dicts dropped, log_indels kept) → run the full fresh-download → variant-search → CPS1 e2e on ml007 → only THEN flip the default in a later alpha + retire the dict index. The live `NRG_3_`/`NGG_3_` dict indexes stay UNTOUCHED until the flip. Prereqs: C2a publish/download dev (task #105) DONE (`b3ac992`); ONLY genome-wide emission on ml007 remains (`/srv/local/lp698/emit_all.log`, expect 23 `.bin` in registry_ + genotypes_; watcher running). C2b needs the FULL branch code (b3ac992: crisprme.py + PostProcess/crisprme_hf.py) shipped to ml007 — branch_pp_p3d only has the 9a37e73 PostProcess, so re-ship crisprme.py + crisprme_hf.py before running `publish-index --dictless`. Then optional Phase-2 refinements (alt-order hint, gt-tier compression, #99 Tier-0 compaction).
- **ml007 emission progress check:** `ssh ml007.research.partners.org 'tail -5 /srv/local/lp698/emit_all.log; ls /srv/local/lp698/e2e_a27/Dictionaries/registry_hg38_1000G_HGDP/*.bin | wc -l; ls /srv/local/lp698/e2e_a27/Dictionaries/genotypes_hg38_1000G_HGDP/*.bin | wc -l'` (done when log ends `ALL DONE` + both counts = 23). Branch PostProcess shipped to `/srv/local/lp698/branch_pp_p3d`.
- **⚠️ NEW CRITICAL open bug (issue #172):** all INDEL off-targets silently dropped since v2.1.9 — `post_analisi_indel.sh:39-40` uses `grep -F -w $chrom` on fake-chromosome (`fakechr22`) targets → empty subset, exit 0, no error. Present on classic main + 2.1.x + **shipped crisprme-plus alpha**. Fix = `grep -F -w "$fakechrom"` both lines + regression fixture + stdout warning; apply in BOTH repos; consider 2.1.x + alpha patch. See memory [[indel-offtargets-silently-dropped-grep-fakechr]]. Awaiting user go-ahead to fix.

## Repos / branches
- `/Users/lp698/Projects/crisprme-plus` — main dev repo. Branch **`feat/tier0-registry`** (dictless work). `main` = shipped (alpha.29). `explore/dictless-variant-analysis` = design doc **v3** (`docs/DICTLESS_VARIANT_ANALYSIS_DESIGN.md`).
- `/Users/lp698/Projects/crisprme-classic` — classic `pinellolab/CRISPRme` repo (GitHub Pages landing `docs/index.html`). Landing is LIVE at alpha.29 + both NRG/NGG + tile bg + logo-only header (PRs #168/#169/#170/#171 merged).
- Docker image repo: `pinellolab/crisprme` (multi-arch, docker-multiarch.yml). HF data: `lucapinello/crisprme-data` (PUBLIC; downloads tokenless).

## DICTLESS redesign — status (branch feat/tier0-registry)
Architecture (design v3): **Tier 0** = tiny always-shipped per-(pos,alt) registry (detection + corrected AF + rsID + per-(db×subpop)/global AC/AN). **Tier 1 (genotype tier)** = compact per-sample carrier store (reconstructs the Samples column; enables combination-aware population freq). Replaces the 152 GB per-sample SNP dicts.

Modules (all `PostProcess/`, stdlib-only, mmap+bisect, ~95 unit tests, all wired into `.github/workflows/unit-tests.yml`):
- `tier0_registry.py` — aggregator (ploidy/sex/AN-aware, full-panel) + compact binary format **v2** (record 16B, group entry 11B, u16 auto-widen u32, VERSION 2) + `RegistryReader` (lookup/rsid/ref/alts_at/derive_record_stats).
- `tier0_compile.py` — dict→registry compiler (`compile_from_dict`, full-panel AN, chrY female-excluded, `*`-alt skipped).
- `simple_analysis_registry.py` — pure `retrieve_5tuple` (selection matrix). **registry+dict = legacy decode + AF-override** (byte-reproduces legacy except AF); registry-only+gtreader = **dictless-full**; registry-only+no-gt = degraded []; no-registry = byte-identical legacy.
- `new_simple_analysis.py` — WIRED: guarded imports; `_resolve_registry_paths` + `_resolve_genotype_paths`; module-level `myreg`/`mygt` opened from `Dictionaries/registry_<vcf>/reg_<chrom>.bin+.idx` and `Dictionaries/genotypes_<vcf>/gt_<chrom>.bin+.idx`.
- `tier1_genotypes.py` — genotype store (MAGIC T1GT; per-(pos,alt) carriers as delta-varint sample-idx + u8 GT code; global sample axis + GT vocab in manifest) + `GenotypeReader.carrier_tokens(pos,alt)` reconstructs exact `sampleID:genotype` list + `compile_genotypes[_from_dict]`.
- `population_summary.py` — **Phase 3a/3b (`4ff3ffe`)**: `summarize(pos_alts, tier0_reader, gt_reader, axis, ploidy_of, phased)` → per-group (global-deduped/per-db/per-(db×subpop)) allele+carrier freq, hom, abs N, max-subpop AF(+label), observed. k=1 exact from Tier0; k≥2 co-occurrence from gt-tier (phased=cis, unphased=assume-cis upper + Bonferroni lower, allele_freq undefined).
- `population_summary_companion.py` — **Phase 3c (`e26f8d2`)**: pure importable writer. `parse_snp_field` (creating (pos,alt) from `final_line[17]` snp_info via last-3 underscore fields, underscore-contig safe), `resolve_phased` (explicit > gt-tier manifest > detect-from-gt_vocab > conservative unphased), `build_rows`/`write_companion` delegating ALL freq math to `population_summary.summarize`. In `new_simple_analysis.py`: read-only `_collect_variant_off_target` hook (gated on `myreg` so legacy path is allocation-identical) + gated+guarded `_write_population_summary_companion` at both exits → `<output>.population_summary.tsv` (identity join key + creating SNP + global/per-db/per-subpop freqs + max-subpop AF+label + observed + labeled unphased carrier lower bound). Companion is a SUPERSET of bestMerge variant rows (left-join FROM bestMerge). `test_popsummary_wiring.py` (19 tests) + CI.

Commits on feat/tier0-registry (chron): tier0 core `c5658f0`; compiler `4487667`; compaction v2 `42cf3d6`; wiring(AF-override) `12b1e4d`; tier1 core `6b5641e`; tier1 wiring `cdd1104`; population_summary `4ff3ffe`; **Phase 3c companion `e26f8d2`**; **Phase 3d build-emit `11f3a75`**; **build-emit minors `c2c766a`**.

## Classic-repo maintenance (parallel thread, 2026-08-17)
- **#172 indel fix** pushed as branch `fix/indel-offtarget-silent-drop` in BOTH repos: crisprme-plus `153f297`, classic (`pinellolab/CRISPRme`) `bf91260`. Fix = `grep -F -w "$fakechrom"` on both lines of `post_analisi_indel.sh` + restored `NF>=10` guard + stdout empty-subset warning + regression test (`test_indel_chrom_subset.py`, 4 tests, CI-wired; classic got a new minimal unit-tests.yml). **PENDING USER APPROVAL:** post the drafted #172 reply, open the PRs, cherry-pick onto `feat/tier0-registry`. Real indel e2e on ml007 still recommended before shipping.
- **Classic PRs merged** (authorized): #148 (VCF-merge tooling, squash), #139 + #137 (docs). #131 left gated.
- **#131 routing** (analysis done): #131 IS the CRISPRme+ platform → keep gated, belongs in crisprme-plus (not classic 2.2.0-dev). Standalone bug fixes to BACK-PORT to classic (verify not-already-present first): stderr-fatal/zero-hit guard, #143 data-loss-on-failure, blank-AF/column-shift robustness, `process_summaries.py` distribution-array `IndexError`, `post_analysis_only.sh` path fix, `gunzip -kf`, `sys.executable`. Security: `send_mail.py` hardcoded app-password `tlgylyubfalteffs` → env vars (Gmail disabled so auth moot, but scrub from current code). **PENDING USER DECISION:** proceed with a consolidated classic back-port PR?

### Validation done (all on ml007, real chr22, via alpha.29 SIF + bind-mounted branch PostProcess)
- **Phase 1 golden diff** (registry+dict vs legacy CPS1 search): 0 detection / 0 Samples-set / 0 rsID / 0 Var_uniq diffs; only AF changed (corrected). (Order-only Samples diffs = hash-seed set-serialization, benign.)
- **Real-chr22 golden harness**: registry compiled 1.5M records; 3000-record REPRODUCE vs independent brute force = 0 mismatches. AF cross-check vs dict's own pipeline AF: median|diff| 0.0009 — and revealed the legacy dict AF field is **EMPTY for ~95% of variants + mis-polarized** for a chunk (so re-derived AF is a real improvement; do NOT gate on reproducing the legacy AF).
- **Compaction**: chr22 225MB→114MB (~2×); genome ~5–7GB.
- **Phase 2 DICTLESS golden diff** (chr22 empty-dict + registry + gt-tier vs legacy): **0 real Samples/rsID/Var_uniq diffs** (carriers reconstructed from gt-tier, NO dict); AF corrected; **+1 boundary bulge target** (chr22:12692399, carriers correct) from alt-order sensitivity. gt-tier chr22 ~370MB (vs dict 2.19GB plain/390MB gz); genome ~22GB gt-tier + ~7GB registry ≈ 29GB vs 152GB (~5×), random-access, correct AF.

## OPEN ITEMS / next steps (priority order)
0. **⚠️ Issue #172 (CRITICAL, verified)** — fix indel off-targets silently dropped: `post_analisi_indel.sh:39-40` → `grep -F -w "$fakechrom"` in BOTH repos + regression fixture + stdout empty-subset warning; reply on #172 (author offered a PR); consider 2.1.x + alpha patch release. AWAITING USER GO-AHEAD.
1. **~~Phase 3c~~ DONE (`e26f8d2`)** — companion output shipped (additive, gated, reviewed). Optional follow-up: real-data e2e that the companion TSV is populated + joinable on a dictless install; extend the web PopulationDistribution to read it.
2. **Alt-order hint** (build-time): store the dict's alt `$`-order so the dictless path byte-matches legacy detection (eliminates the 1 boundary bulge target). Order-sensitivity is arguably a latent legacy bug.
3. **gt-tier small-block compression** (stdlib gzip/zlib blocks) → below the 31GB gz dict; **#99 Tier-0 `derivable_db_global` compaction** → ~1–2GB.
4. **Build/publish/ship**: make `crisprme.py build-index-only` (crisprme.py ~1811-1863) EMIT `registry_<vcf>/reg_<chrom>.bin+.idx` + `genotypes_<vcf>/gt_<chrom>.bin+.idx`; publish to HF (Tier0 in main tarball, genotype tier as separate optional `genotypes_<db>.tar.gz`); real dictless new-user e2e (download → variant search → CPS1 w/ reconstructed Samples); ship in a future alpha.
5. **line-404 emission guard**: registry-only (no dict, no gt-tier) drops variant rows via `if len(samples)>0` — relax/adjust for a sensible registry-only mode.
6. **#97 follow-up**: `crisprme.py _sort_annotation`/`_process_personal_annotation` write temps next to a `--annotation` input + rewrite in place → fails on a read-only dir (apptainer-only edge; Docker/web/batteries path is writable, so non-blocking).
7. Consider **PR** for Phase 1+2+3a/b (CI-gated) when ready.

## alpha.29 caveats to tell the collaborator
Open alpha. The single **`AF` column is weak** (legacy dict AF empty/mis-polarized) — lean on **CFD/CFD_ref, Var_uniq, Samples, PopulationDistribution**. Post-analysis functional but not fast. (All fixed/improved by the dictless work in progress.)

## ml007 infra (FQDN ml007.research.partners.org; apptainer, NOT docker; avoid /tmp)
- SIF: `/srv/local/lp698/crisprme_a29.sif` (alpha.29). Run branch code by bind-mounting: `apptainer exec --writable-tmpfs --env PATH=/opt/conda/bin:... --env LD_LIBRARY_PATH=/opt/conda/lib -B /srv/local/lp698 -B /srv/local/lp698/branch_postprocess:/opt/conda/opt/crisprme/PostProcess <SIF> ...`.
- `/srv/local/lp698/branch_postprocess/` = tar of the branch PostProcess (re-ship after edits: `tar czf ... PostProcess` → scp → extract).
- `/srv/local/lp698/e2e_a27/` = full install (genome/indexes/dicts/samplesID/Annotations). Has `Dictionaries/registry_hg38_1000G_HGDP/reg_chr22.bin+.idx` + `genotypes_hg38_1000G_HGDP/gt_chr22.bin+.idx` placed. `Results/regdiff_leg` = legacy baseline; `regdiff_reg2`/`regdiff_dictless` = registry/dictless runs.
- `/srv/local/lp698/tier0/` = harness scripts (`golden_chr22.py`, `afcheck_chr22.py`, `golden_pipeline_diff2.py`, `run_golden_*.sh`) + `out/reg_chr22.bin/.idx.json`.
- HF token cached at `~/.cache/huggingface/token` (write access to lucapinello/crisprme-data). **ml007 crisprme_220_val/env is STALE** — never use it; use the SIF.
- No bcftools/plink on ml007 or the Mac (external-oracle used = dict's own pipeline AF field).

## Key memories (in /Users/lp698/.claude/.../memory/)
crisprme-plus-dictless-population-design.md (v3 semantics + AF-field finding + validation), crisprme-plus-batteries-variant-index-dict-gap.md (RESOLVED), crisprme-ml007-valenv-is-stale.md, crisprme-plus-nrg-default-decision.md, crisprme-plus-checkout-location, conda-mirror-prefix-dev.

## Security (USER-side, resolved/scheduled)
Gmail app password: account DISABLED (moot). Docker Hub token: user rotating next week.

## No background jobs running as of this write. Tests: `cd PostProcess && python3 -m unittest test_tier0_registry test_tier0_compile test_new_simple_analysis_registry test_tier1_genotypes test_tier1_wiring test_population_summary` (all green).
